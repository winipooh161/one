{{-- <div class="pwa-install-container" style="display: none;" id="pwa-install-container">
    <button id="install-pwa" class="btn btn-outline-primary d-flex align-items-center" style="display: none;">
        <i class="fas fa-download me-2"></i> Установить приложение
    </button>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Проверяем, запущено ли приложение в режиме standalone или fullscreen
        if (window.matchMedia('(display-mode: standalone)').matches || 
            window.matchMedia('(display-mode: fullscreen)').matches) {
            // Приложение уже установлено, скрываем кнопку
            document.getElementById('pwa-install-container').style.display = 'none';
        } else {
            // Показываем контейнер кнопки
            document.getElementById('pwa-install-container').style.display = 'block';
        }
    });
</script> --}}
